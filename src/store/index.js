/*
 * 模块: 全局状态仓库
 * 作用: 提供 Vuex Store 容器（当前未定义具体 state/mutation/action，可按需扩展）
 */
import { createStore } from 'vuex'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
